spring:
  application:
    name: restaurant-order
  cloud:
    nacos:
      config:
        server-addr: 192.168.200.101:8848
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD}
        file-extension: yaml
      discovery:
        server-addr: 192.168.200.101:8848
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD}
